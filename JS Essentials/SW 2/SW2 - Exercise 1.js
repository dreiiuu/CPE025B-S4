// We declared the varaible "end" and assigned a value of 2.
// This loop runs and prints all the numbers from 1 to end (which is 2).
// It starts at 1 and increments by 1 each time, printing the current value of i.

let end = 2; 
for(let i=1; i <= end; i++) {
    console.log(i); 
}
